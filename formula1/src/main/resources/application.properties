spring.application.name=backend

# Supabase PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://db.nsevqazzkcqldyuedbly.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=7324*Tan
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool Settings (recommended for Supabase)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

# JWT Configuration
# jwt.secret=duong-quoc-tan-formula1-postgresql-nextjs-nestjs
# jwt.expiration=86400000

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cutorasusu2004@gmail.com
spring.mail.password=cnid lavl ifsq dbnu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Application Configuration
# app.frontend.url=${FRONTEND_URL:http://localhost:3000}